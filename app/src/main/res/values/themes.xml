<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PiAF" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ActionBar.Piaf" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="background">@color/colorPrimary</item>
    </style>

    <style name="Theme.PopUpActionBar.Piaf" parent="ThemeOverlay.AppCompat.Light">
        <item name="background">@color/colorAccent</item>
        <item name="android:textColor">@color/white</item>
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownVerticalOffset">-4dp</item>
    </style>

    <style name="Theme.PiAF" parent="Base.Theme.PiAF" />

    <style name="ThemeOverlay.PiAF.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>